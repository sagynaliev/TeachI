cat > render.yaml << 'EOF'
services:
  - type: web
    name: teachi-3
    env: node
    rootDirectory: TeachI/backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
  - key: MONGODB_URI
    value: mongodb+srv://240118001_db_user:<db_password>@teachi.6bvwzai.mongodb.net/?appName=TeachI
      - key: NODE_ENV
        value: production
EOF
