cat > render.yaml << 'EOF'
services:
  - type: web
    name: teachi-3
    env: node
    rootDirectory: TeachI/backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
  - key: MONGODB_URI
    value: mongodb+srv://teachi_render:Render2024@teachi.6bvwzai.mongodb.net/?retryWrites=true&w=majority&appName=TeachI
      - key: NODE_ENV
        value: production
EOF
